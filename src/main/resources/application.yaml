spring:
  application:
    name: back
  profiles:
    active: dev
  output:
    ansi:
      enabled: ALWAYS
  jackson:
    serialization:
      fail-on-empty-beans: false
logging:
  level:
    com:
      back: debug
server:
  port: 8080